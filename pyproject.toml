[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "langflow-mcp"
version = "0.1.0"
description = "MCP server for Langflow HTTP API."
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
authors = [{ name = "taka392" }]
keywords = ["mcp", "langflow", "cursor"]
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3 :: Only",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
dependencies = [
    "mcp[cli]>=1.0.0",
    "requests>=2.31.0",
]

[project.scripts]
langflow-mcp = "langflow_mcp.server:main"
langflow-mcp-check = "langflow_mcp.check:main"

[project.urls]
Repository = "https://github.com/taka392/langflow-mcp"

[tool.setuptools.packages.find]
include = ["langflow_mcp*"]
